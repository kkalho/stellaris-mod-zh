# 项目路线图（ROADMAP）

> 更新：2026-08-30 · 配合 `PROJECT_HANDOFF.md` 使用——交接文档回答"怎么做"，本文回答"接下来做什么"。
> 原则：**数据真实优先于功能**；每批扩容必须过 `rebuild_all` + `verify_db`（CI 已兜底）；不破"单文件、无构建"底线。

## 现状基线（2026-08-30）

- 群星 777/1020（batch17-19 完成，下一批 #778），六字段 100%；CK3 300（版本链 1.13-1.19 已建）；HOI4 空框架
- 数据舰桥启动页 + 银河星图 + 主题系统（青/金/钢灰）已上线公网
- 工程设施齐备：CI（重建+体检）、Uptime 拨测、verify_db、export/apply 云同步、TAT 通道

## 短期（随接下来 2-3 批扩容完成）

| # | 事项 | 说明 |
|---|---|---|
| 1 | **扩容至 1020** | 剩余 243 个 ≈ 5 批。单批节奏：抓取 3min + 3 子智能体翻译 ~25min + 收敛/推送/云同步 10min |
| 2 | **CK3 云同步泛化** | export_cloud_sync / apply_cloud_sync 从群星专用扩为多游戏（--game + 静态路径表），一次 TAT 同步全部游戏；当前 CK3 版本标注仅本地生效 |
| 3 | **老批次 reviews 质量回检** | batch2/8/9（#1-277）存在「官方数据显示 5 星好评」等超出"作者自述"范畴的表述，违反数据真实原则第 3 条；约 277 条，子智能体分批复核改写 |
| 4 | 前端小项 | 详情页全屏视图（隐藏首页家具）、SVG 图标替换 emoji、galaxy 配色随主题微调 |

## 玩家体验增强（2026-08-30 玩家视角巡检产出）

> 以玩家高频问题排序：「有没有中文 → 旧版能不能用 → 弃坑了用什么 → 和谁冲突 → 有没有遗珠」

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| P1 | **中文支持标注** | ✅ 已上线（详情格+列表徽章，实测鸽组汉化显示「有汉化包」） | 详情页「中文支持」格（有汉化包/自带中文/未见标注）+ 列表卡「中」徽章；数据驱动（localization.json + Translation 标签） |
| P2 | 旧版风险分级 | ✅ 已上线 | 详情页「旧版风险提示」格：显式适配旧版/时间推断旧版 × 视觉类(低风险)/机制类(可能不兼容)，当前时代自动不提示 |
| P3 | 弃坑替代推荐 | 🔶 基础已建 | mine_compat 已从弃坑描述挖 successor 进 compat.notes（仅弃坑条目，防方向歧义）；前端弃坑徽章悬浮展示待做 |
| P4 | 依赖前置结构化 | 🔶 基础已建 | mine_compat 挖出 requires 关系入 compat 表（详情页「依赖前置」自动展示）；示例：巨构依赖 Universal Resource Patch |
| P5 | 遗珠榜 | 待做 | 订阅排序有马太效应；收藏率高+近期更新+订阅中游 = 高品质低订阅入口 |
| P6 | 活跃徽章 | 待做 | 「30/90 天内更新过」小绿点比更新日期直观十倍 |
| P7 | 我的清单冲突检测 | ✅ 已上线 | 工具栏「⚔ 冲突检测」面板：粘贴 ID/链接或用当前筛选结果 → 两两冲突配对（附证据）+ 缺失依赖警告 + 覆盖率如实提示；后端 /api/<game>/conflict-check |
| P8 | 清单分享页 | 待做 | URL 编码 ID 列表 → 打开即清单总览（总订阅/冲突提示），社区 build 分享场景 |

## 中期（1-2 个月）

| # | 事项 | 说明 |
|---|---|---|
| 5 | CK3 专属界面 | 金色纹章主题深化 + CK3 特有维度（人物线/家族/事件年代），超越通用列表 |
| 6 | HOI4 收录启动 | 复用 CK3 脚本链改 app_id 394360，先 Top 300 + 六字段 |
| 7 | 第一局推荐（人工评测） | 真正的新手评测内容：每条附社区来源，WebSearch 核实，禁止编造 |
| 8 | 组合推荐器 | 基于 compat 矩阵 + 订阅相关性做「装了 A 的人也在装 B」，数据都在库里 |

## 长期 / 待定

- **反馈通道**：页面"报错/补充信息"入口 → GitHub Issues 模板（需登录门槛考量）
- **PWA/数据快照**：离线可查，弱网友好
- **更多游戏**（EU4/VIC3）：`games/` 注册制架构已就绪，按需求推进

## 协作与运维注意（2026-08-30 新增）

- **并行会话同时操作云端**：journal 记录到非本会话发起的服务重启；云端同步前先查最近 TAT 调用（`tccli tat DescribeInvocationTasks`），或建立"认领制"（交接文档标注认领人）
- **大存档走 .gz 副本**：4.7MB 明文两次 TAT 超时，1.07MB .gz 一次成功；`export_cloud_sync` 产物建议始终 gzip 提交
- 本地测试服务（8099）用完即关，避免用户浏览器指向本地端口时误判"站点挂了"

## 已知数据/工程债

- 老批次 reviews 表述问题（见短期待办 #3，本轮 UI 巡检时发现）
- `details.jsonl` 30 行历史重复（无害，导入按 ID 去重）
- CK3 云端落后本地一个版本链（短期待办 #2 解决）
- `D:\Projects\temp\stellaris-mod-zh-deploy\` 旧副本待清理（确认无用后删除）
